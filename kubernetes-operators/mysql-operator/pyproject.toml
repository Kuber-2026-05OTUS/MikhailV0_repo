[project]
name = "mysql-operator"
version = "0.1.0"
description = "Simple Kopf operator for MySQL custom resources"
requires-python = ">=3.12"
dependencies = [
    "kopf>=1.37.4",
    "kubernetes>=30.1.0",
]

[tool.uv]
package = false
