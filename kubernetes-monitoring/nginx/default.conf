server {
    listen 80;
    server_name _;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location = /basic_status {
        stub_status;
        access_log off;
        allow all;
    }
}

